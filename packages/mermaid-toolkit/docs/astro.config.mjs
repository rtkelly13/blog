// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://rtkelly13.github.io',
  base: '/blog/mermaid-toolkit',
  build: {
    assets: 'assets',
  },
});
